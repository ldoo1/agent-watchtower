# Slack Webhook URL for sending alerts
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Optional: Minimum log level to capture (default: error)
# LOG_LEVEL=error

# Optional: Debounce window in milliseconds (default: 300000 = 5 minutes)
# DEBOUNCE_MS=300000

# Optional: Ring buffer size (default: 50 lines)
# BUFFER_SIZE=50
